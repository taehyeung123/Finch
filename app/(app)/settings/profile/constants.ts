/**
 * 탈퇴 확인 문구 — 이메일이 있으면 이메일, 없으면 이 문구를 타이핑한다.
 *
 * 카카오는 이메일 제공이 **선택 동의**라 user.email 이 비어 오는 계정이 실제로 있다.
 * 이메일만 받겠다고 하면 그 사람들은 탈퇴가 영구히 막히는데, 개인정보처리방침은
 * "회원 탈퇴로 동의를 철회할 수 있다"고 약속하고 있다 — 약속을 못 지키게 된다.
 *
 * 이 파일이 따로 있는 이유: actions.ts 는 "use server" 라 **async 함수 외에는
 * 아무것도 export 할 수 없다**(상수도 안 된다). 화면과 서버가 같은 문구를 봐야
 * 하므로 양쪽이 함께 읽는 자리를 둔다 — 문자열을 두 곳에 복사하면 언젠가 갈린다.
 */
export const DELETE_PHRASE = "탈퇴합니다";
